# Supabase (get from Supabase project settings → API)
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Database — pooler connection (Supabase: Settings → Database → Connection Pooling → URI)
# Use this for serverless environments (Vercel). Add ?pgbouncer=true
DATABASE_URL=postgresql://postgres.YOUR_PROJECT_REF:PASSWORD@aws-0-REGION.pooler.supabase.com:6543/postgres?pgbouncer=true

# Direct database connection (Supabase: Settings → Database → Connection string → URI)
# Used by Prisma for migrations only. Not needed on Vercel.
DIRECT_URL=postgresql://postgres:PASSWORD@db.YOUR_PROJECT_REF.supabase.co:5432/postgres

# Optional: server-only Supabase service role (for admin operations)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
