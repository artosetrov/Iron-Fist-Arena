# Supabase (get from Supabase project settings → API)
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Database (Supabase: Settings → Database → Connection string, URI). If TLS error, add ?sslmode=require
DATABASE_URL=postgresql://postgres:PASSWORD@db.YOUR_PROJECT_REF.supabase.co:5432/postgres

# Optional: server-only Supabase service role (for admin operations)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
